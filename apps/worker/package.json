{
  "name": "@via/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun --env-file=../../.env test",
    "rebuild-line-schemas": "bun --env-file=../../.env src/line-schema/rebuild.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@via/db": "workspace:*",
    "csv-parse": "^6.1.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  }
}
