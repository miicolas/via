import { RoutePlaceholder } from '@/components/route-placeholder';

export default function SignInScreen() {
  return (
    <RoutePlaceholder
      title="Connexion"
      description="LՎcran de connexion sera construit ici."
    />
  );
}
